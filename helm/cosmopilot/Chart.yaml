apiVersion: v2
name: cosmopilot
# version and appVersion are placeholders: at release time `make helm.package` sets both to the
# version of the cosmopilot v* tag, so each release publishes the chart with the same version as the
# app, and the chart deploys that app version by default.
version: 0.0.0
appVersion: 0.0.0
