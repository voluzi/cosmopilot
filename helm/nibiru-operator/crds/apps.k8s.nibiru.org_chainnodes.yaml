---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.3
  creationTimestamp: null
  name: chainnodes.apps.k8s.nibiru.org
spec:
  group: apps.k8s.nibiru.org
  names:
    kind: ChainNode
    listKind: ChainNodeList
    plural: chainnodes
    singular: chainnode
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.chainID
      name: ChainID
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: ChainNode is the Schema for the chainnodes API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ChainNodeSpec defines the desired state of ChainNode
            properties:
              app:
                description: App specifies image and binary name of the chain application
                  to run
                properties:
                  app:
                    description: App is the name of the binary of the application
                      to be run
                    type: string
                  image:
                    description: Image indicates the docker image to be used
                    minLength: 1
                    type: string
                  imagePullPolicy:
                    description: ImagePullPolicy indicates the desired pull policy
                      when creating nodes. Defaults to `Always` if `version` is `latest`
                      and `IfNotPresent` otherwise.
                    type: string
                  version:
                    description: Version is the image tag to be used
                    type: string
                required:
                - app
                - image
                type: object
              config:
                description: Config allows setting specific configurations for this
                  node
                properties:
                  override:
                    description: Override allows overriding configs on toml configuration
                      files
                    x-kubernetes-preserve-unknown-fields: true
                type: object
              genesis:
                description: Genesis indicates where this node will get the genesis
                  from
                properties:
                  url:
                    description: URL to download the genesis from.
                    minLength: 1
                    type: string
                type: object
              persistence:
                description: Persistence configures pvc for persisting data on nodes
                properties:
                  size:
                    description: Size of the persistent volume for storing data. Defaults
                      to `50Gi`. Ignored  if autoresize is enabled.
                    minLength: 1
                    type: string
                  storageClass:
                    description: StorageClassName specifies the name of the storage
                      class to use to create persistent volumes.
                    type: string
                type: object
            required:
            - app
            - genesis
            type: object
          status:
            description: ChainNodeStatus defines the observed state of ChainNode
            properties:
              chainID:
                description: ChainID shows the chain ID
                type: string
              nodeID:
                description: NodeID show this node's ID
                type: string
              pvcSize:
                description: PvcSize shows the current size of the pvc of this node
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
